{% extends 'layout.html.twig' %}
{% block head %}
    {{parent()}}
    <script type="text/javascript" src="js/views/index.js"></script>
{% endblock %}
{% block body %}
	{{ parent() }}
    {% for item in data %}
        <article class="{{item.class}}" data-id="{{item.id}}" data-weight="{{item.weight}}">
            {{item.type}}
        </article>
    {% endfor %}
{% endblock %}
